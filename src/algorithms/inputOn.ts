export default function inputOn()
{
        (document.getElementById("sortingButton") as HTMLButtonElement).disabled = false;
        (document.getElementById("generateButton") as HTMLButtonElement).disabled = false;
}